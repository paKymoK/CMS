# docker-compose reads a .env file from this directory automatically. Copy this to .env and
# fill in real values for local development — never commit the real .env file.

# Required for chat-service's public CMS assistant (PublicAssistantService) — get one from
# https://console.anthropic.com/
ANTHROPIC_API_KEY=
